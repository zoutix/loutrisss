/* =====================================================================
   LOUTRIS — js/supabase-config.js
   Cloud sync credentials (publishable key — safe to expose in clients).
   If url/key are blank, the game runs fully offline as before.
   ===================================================================== */
(function (global) {
  "use strict";
  global.LOUTRIS_SUPABASE = {
    url: "https://thdigaamevrykuyyrgpn.supabase.co",
    publishableKey: "sb_publishable__jp_CfbAm9MAaLP8ZgTv0A_QSFBAoDV"
  };
})(window);
