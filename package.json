{
  "name": "loutris",
  "private": true,
  "version": "1.0.0",
  "description": "Loutris word game",
  "scripts": {
    "syntax:check": "node scripts/check-syntax.js",
    "check:security": "node scripts/check-security.js",
    "check:supabase-contract": "node scripts/check-supabase-contract.js",
    "generate:word-seed": "node scripts/generate-word-seed.js",
    "check:dom": "node scripts/check-dom-wiring.js",
    "test": "node --test tests/**/*.test.js",
    "check": "npm run syntax:check && npm run check:security && npm run check:supabase-contract && npm run check:dom && npm test"
  }
}
